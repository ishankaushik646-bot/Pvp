repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local environment = (type(getgenv) == "function" and getgenv()) or _G

local isMobile = UserInputService.TouchEnabled and not UserInputService.MouseEnabled
local UI_SCALE = isMobile and 0.9 or 1.15
local BASE_WIDTH = 268 * UI_SCALE
local BASE_HEIGHT = 178 * UI_SCALE
local MINIMIZED_HEIGHT = 44 * UI_SCALE
local FONT_SCALE = UI_SCALE

local COLORS = {
	Base = Color3.fromRGB(255, 241, 246),
	Card = Color3.fromRGB(255, 255, 255),
	Accent = Color3.fromRGB(255, 133, 165),
	On = Color3.fromRGB(255, 118, 154),
	Off = Color3.fromRGB(213, 176, 189),
	Text = Color3.fromRGB(92, 48, 66),
	Sub = Color3.fromRGB(146, 108, 124),
	Stroke = Color3.fromRGB(255, 170, 195),
}

pcall(function()
	for _, name in ipairs({ "NSFW Hub", "Cursed Hub", "Cursed Hub Anti Bat" }) do
		local old = PlayerGui:FindFirstChild(name)
		if old then old:Destroy() end
	end
end)

pcall(function()
	local prev = environment.__NSFW_ANTITP_RUNTIME
	if type(prev) == "table" and type(prev.destroy) == "function" then
		pcall(prev.destroy)
	end
end)

local AntiTpEnabled = false
local Bind = Enum.KeyCode.F

local function tween(obj, time, props)
	local t = TweenService:Create(obj, TweenInfo.new(time, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), props)
	t:Play()
	return t
end

local function saveConfig()
	pcall(function()
		if not writefile then return end
		writefile("NSFW_AntiTPBat_Config.json", HttpService:JSONEncode({
			AntiTpBat = AntiTpEnabled,
			Bind = Bind.Name,
		}))
	end)
end

local function loadConfig()
	pcall(function()
		if not isfile or not isfile("NSFW_AntiTPBat_Config.json") then return end
		local data = HttpService:JSONDecode(readfile("NSFW_AntiTPBat_Config.json"))
		if type(data) ~= "table" then return end
		if data.AntiTpBat ~= nil then AntiTpEnabled = data.AntiTpBat == true end
		if type(data.Bind) == "string" then
			local ok, code = pcall(function() return Enum.KeyCode[data.Bind] end)
			if ok and typeof(code) == "EnumItem" then Bind = code end
		end
	end)
end

local function fetchBytes(url)
	local requestFn = rawget(environment, "request")
		or rawget(environment, "http_request")
		or rawget(environment, "httprequest")
	if type(requestFn) ~= "function" then
		local syn = rawget(environment, "syn")
		if type(syn) == "table" then requestFn = rawget(syn, "request") end
	end
	if type(requestFn) ~= "function" then
		local http = rawget(environment, "http")
		if type(http) == "table" then requestFn = rawget(http, "request") end
	end
	if type(requestFn) == "function" then
		local ok, res = pcall(requestFn, { Url = url, Method = "GET" })
		if ok and type(res) == "table" then
			local body = res.Body or res.body
			if type(body) == "string" and #body > 0 then return body end
		end
	end
	local ok, body = pcall(function() return game:HttpGet(url) end)
	if ok and type(body) == "string" and #body > 0 then return body end
	return nil
end

local function resolveBackground()
	local toAsset = rawget(environment, "getcustomasset")
	if type(toAsset) ~= "function" then
		toAsset = rawget(environment, "getsynasset")
	end
	if type(toAsset) ~= "function" then return nil end

	local fresh = false
	if isfile and isfile("NSFW_AntiTPBat/background.png") then
		if type(getfilesize) == "function" then
			local ok, size = pcall(getfilesize, "NSFW_AntiTPBat/background.png")
			if ok and type(size) == "number" and size > 1024 then fresh = true end
		else
			fresh = true
		end
	end

	if fresh then
		local ok, asset = pcall(toAsset, "NSFW_AntiTPBat/background.png")
		if ok and type(asset) == "string" and #asset > 0 then return asset end
	end

	local data = fetchBytes("https://files.catbox.moe/8htxh4.png")
	if type(data) ~= "string" or #data < 1024 then return nil end

	if type(writefile) ~= "function" then return nil end
	if makefolder and not (isfolder and isfolder("NSFW_AntiTPBat")) then
		pcall(makefolder, "NSFW_AntiTPBat")
	end

	local wrote = pcall(writefile, "NSFW_AntiTPBat/background.png", data)
	if wrote and isfile and isfile("NSFW_AntiTPBat/background.png") then
		local ok, asset = pcall(toAsset, "NSFW_AntiTPBat/background.png")
		if ok and type(asset) == "string" and #asset > 0 then return asset end
	end
	return nil
end

local FAKE_ROOT_Y = 1e8
local FAKE_ROOT_VELOCITY = Vector3.zero

local runtime = {
	alive = true,
	enabled = false,
	character = nil,
	rootPart = nil,
	fakeRoot = nil,
	stepConnection = nil,
}
environment.__NSFW_ANTITP_RUNTIME = runtime

local function findGlobalFunction(...)
	for i = 1, select("#", ...) do
		local name = select(i, ...)
		local v = rawget(environment, name)
		if type(v) == "function" then return v end
		v = rawget(_G, name)
		if type(v) == "function" then return v end
	end
	return nil
end

local function setHidden(instance, property, value)
	if not instance then return false end
	local setter = findGlobalFunction("sethiddenproperty", "set_hidden_property", "sethiddenprop")
	if setter then
		local ok = pcall(setter, instance, property, value)
		if ok then return true end
	end
	return pcall(function() instance[property] = value end)
end

local function getHidden(instance, property)
	if not instance then return false, nil end
	local getter = findGlobalFunction("gethiddenproperty", "get_hidden_property", "gethiddenprop")
	if getter then
		local ok, value = pcall(getter, instance, property)
		if ok then return true, value end
	end
	local ok, value = pcall(function() return instance[property] end)
	return ok, value
end

local function isBasePart(inst)
	if not inst then return false end
	local ok, r = pcall(function() return inst:IsA("BasePart") end)
	return ok and r == true
end

local function getCurrentRoot(character)
	character = character or LocalPlayer.Character
	if not character then return nil end
	local ok, root = pcall(function() return character:FindFirstChild("HumanoidRootPart") end)
	if ok and isBasePart(root) then return root end
	return nil
end

local function destroyFakeRoot()
	if runtime.fakeRoot then
		pcall(function() runtime.fakeRoot:Destroy() end)
		runtime.fakeRoot = nil
	end
end

local function restoreReplicationRoot()
	local root = runtime.rootPart
	if isBasePart(root) then
		setHidden(root, "PhysicsRepRootPart", root)
	end
end

local function createFakeRoot(rootPart)
	destroyFakeRoot()
	if not isBasePart(rootPart) then return nil end
	local fake = Instance.new("Part")
	fake.Name = "NSFWFakeRepRoot"
	fake.Size = Vector3.new(2, 2, 1)
	fake.Transparency = 1
	fake.Anchored = true
	fake.CanCollide = false
	fake.CanQuery = false
	fake.CanTouch = false
	fake.Massless = true
	fake.CFrame = CFrame.new(rootPart.Position.X, FAKE_ROOT_Y, rootPart.Position.Z)
	fake.Parent = workspace
	runtime.fakeRoot = fake
	return fake
end

local function assignFakeReplicationRoot(rootPart, fake)
	if not isBasePart(rootPart) or not isBasePart(fake) then return end
	setHidden(rootPart, "PhysicsRepRootPart", fake)
end

local function stepDesync()
	if not runtime.enabled or not runtime.alive then return end
	local root = runtime.rootPart
	if not isBasePart(root) or not root.Parent then
		root = getCurrentRoot(runtime.character)
		runtime.rootPart = root
		if not root then return end
	end
	if not isBasePart(runtime.fakeRoot) or not runtime.fakeRoot.Parent then
		local fake = createFakeRoot(root)
		if fake then assignFakeReplicationRoot(root, fake) end
		return
	end
	local fake = runtime.fakeRoot
	local ok, rootPos, fakePos = pcall(function()
		return root.Position, fake.Position
	end)
	if ok and (
		math.abs(rootPos.X - fakePos.X) > 0.01
		or math.abs(rootPos.Z - fakePos.Z) > 0.01
		or math.abs(fakePos.Y - FAKE_ROOT_Y) > 0.01
	) then
		pcall(function()
			fake.CFrame = CFrame.new(rootPos.X, FAKE_ROOT_Y, rootPos.Z)
		end)
	end
	pcall(function()
		fake.Anchored = true
		fake.AssemblyLinearVelocity = FAKE_ROOT_VELOCITY
	end)
	local got, current = getHidden(root, "PhysicsRepRootPart")
	if not got or current ~= fake then
		setHidden(root, "PhysicsRepRootPart", fake)
	end
end

local function stopStepConnection()
	if runtime.stepConnection then
		pcall(function() runtime.stepConnection:Disconnect() end)
		runtime.stepConnection = nil
	end
end

local function startStepConnection()
	stopStepConnection()
	runtime.stepConnection = RunService.Stepped:Connect(stepDesync)
end

local function setAntiTp(on)
	on = on == true
	runtime.enabled = on
	if on then
		runtime.character = LocalPlayer.Character
		local root = getCurrentRoot(runtime.character)
		runtime.rootPart = root
		if not root then
			runtime.enabled = false
			AntiTpEnabled = false
			saveConfig()
			return false
		end
		local fake = createFakeRoot(root)
		assignFakeReplicationRoot(root, fake)
		startStepConnection()
	else
		stopStepConnection()
		restoreReplicationRoot()
		destroyFakeRoot()
	end
	AntiTpEnabled = runtime.enabled
	saveConfig()
	return AntiTpEnabled
end

local function bindCharacter(character)
	runtime.character = character
	runtime.rootPart = getCurrentRoot(character)
	if runtime.enabled then
		destroyFakeRoot()
		local root = runtime.rootPart
		if not root and character then
			local ok, waited = pcall(function()
				return character:WaitForChild("HumanoidRootPart", 8)
			end)
			if ok and isBasePart(waited) then
				root = waited
				runtime.rootPart = root
			end
		end
		if root then
			local fake = createFakeRoot(root)
			assignFakeReplicationRoot(root, fake)
			startStepConnection()
		end
	end
end

LocalPlayer.CharacterAdded:Connect(function(char)
	task.defer(bindCharacter, char)
end)
if LocalPlayer.Character then bindCharacter(LocalPlayer.Character) end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NSFW Hub"
ScreenGui.IgnoreGuiInset = true
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 999
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui

local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Active = true
Main.ClipsDescendants = true
Main.AnchorPoint = Vector2.new(0.5, 0.5)
Main.Position = UDim2.new(0.5, 0, 0.5, 0)
Main.Size = UDim2.new(0, BASE_WIDTH, 0, BASE_HEIGHT)
Main.BackgroundColor3 = COLORS.Base
Main.BorderSizePixel = 0
Main.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 14 * UI_SCALE)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = COLORS.Stroke
MainStroke.Thickness = 1 * UI_SCALE
MainStroke.Transparency = 0.45
MainStroke.Parent = Main

local BgImage = Instance.new("ImageLabel")
BgImage.Name = "Background"
BgImage.Size = UDim2.new(1, 0, 1, 0)
BgImage.BackgroundTransparency = 1
BgImage.Image = ""
BgImage.ImageTransparency = 0.32
BgImage.ScaleType = Enum.ScaleType.Crop
BgImage.Parent = Main

local BgCorner = Instance.new("UICorner")
BgCorner.CornerRadius = UDim.new(0, 14 * UI_SCALE)
BgCorner.Parent = BgImage

local Veil = Instance.new("Frame")
Veil.Name = "Veil"
Veil.Size = UDim2.new(1, 0, 1, 0)
Veil.BackgroundColor3 = COLORS.Base
Veil.BackgroundTransparency = 0.42
Veil.BorderSizePixel = 0
Veil.Parent = Main

local VeilCorner = Instance.new("UICorner")
VeilCorner.CornerRadius = UDim.new(0, 14 * UI_SCALE)
VeilCorner.Parent = Veil

task.spawn(function()
	local asset = resolveBackground()
	if asset then
		BgImage.Image = asset
	end
end)

local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.ZIndex = 4
TopBar.Size = UDim2.new(1, 0, 0, 44 * UI_SCALE)
TopBar.BackgroundTransparency = 1
TopBar.Parent = Main

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.ZIndex = 5
Title.Position = UDim2.new(0, 14 * UI_SCALE, 0, 9 * UI_SCALE)
Title.Size = UDim2.new(0, 150 * UI_SCALE, 0, 16 * UI_SCALE)
Title.BackgroundTransparency = 1
Title.Text = "NSFW HUB"
Title.TextColor3 = COLORS.Text
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 13 * FONT_SCALE
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = TopBar

local SubTitle = Instance.new("TextLabel")
SubTitle.Name = "SubTitle"
SubTitle.ZIndex = 5
SubTitle.Position = UDim2.new(0, 14 * UI_SCALE, 0, 25 * UI_SCALE)
SubTitle.Size = UDim2.new(0, 150 * UI_SCALE, 0, 12 * UI_SCALE)
SubTitle.BackgroundTransparency = 1
SubTitle.Text = "Anti TP Bat"
SubTitle.TextColor3 = COLORS.Sub
SubTitle.Font = Enum.Font.Gotham
SubTitle.TextSize = 10 * FONT_SCALE
SubTitle.TextXAlignment = Enum.TextXAlignment.Left
SubTitle.Parent = TopBar

local function makeTopButton(name, glyph, xOffset)
	local btn = Instance.new("TextButton")
	btn.Name = name
	btn.ZIndex = 5
	btn.AnchorPoint = Vector2.new(1, 0.5)
	btn.Position = UDim2.new(1, xOffset * UI_SCALE, 0.5, 0)
	btn.Size = UDim2.new(0, 24 * UI_SCALE, 0, 24 * UI_SCALE)
	btn.BackgroundColor3 = COLORS.Card
	btn.BackgroundTransparency = 0.35
	btn.BorderSizePixel = 0
	btn.Text = glyph
	btn.TextColor3 = COLORS.Text
	btn.TextSize = 12 * FONT_SCALE
	btn.Font = Enum.Font.GothamBold
	btn.AutoButtonColor = false
	btn.Parent = TopBar
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 7 * UI_SCALE)
	btn.MouseEnter:Connect(function()
		tween(btn, 0.15, { BackgroundTransparency = 0.12 })
	end)
	btn.MouseLeave:Connect(function()
		tween(btn, 0.15, { BackgroundTransparency = 0.35 })
	end)
	return btn
end

local MinBtn = makeTopButton("MinBtn", "-", -42 * UI_SCALE)
local CloseBtn = makeTopButton("CloseBtn", "x", -12 * UI_SCALE)

local function makeRow(name, y, height)
	local row = Instance.new("Frame")
	row.Name = name
	row.ZIndex = 4
	row.Position = UDim2.new(0, 12 * UI_SCALE, 0, y * UI_SCALE)
	row.Size = UDim2.new(1, -24 * UI_SCALE, 0, height * UI_SCALE)
	row.BackgroundColor3 = COLORS.Card
	row.BackgroundTransparency = 0.22
	row.BorderSizePixel = 0
	row.Parent = Main
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10 * UI_SCALE)

	local stroke = Instance.new("UIStroke")
	stroke.Color = COLORS.Stroke
	stroke.Thickness = 1 * UI_SCALE
	stroke.Transparency = 0.6
	stroke.Parent = row

	local label = Instance.new("TextLabel")
	label.Name = "Label"
	label.ZIndex = 6
	label.Position = UDim2.new(0, 12 * UI_SCALE, 0, 0)
	label.Size = UDim2.new(0, 140 * UI_SCALE, 1, 0)
	label.BackgroundTransparency = 1
	label.TextColor3 = COLORS.Text
	label.Font = Enum.Font.GothamSemibold
	label.TextSize = 12 * FONT_SCALE
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = row
	return row, label
end

local ToggleRow, ToggleLabel = makeRow("ToggleRow", 52 * UI_SCALE, 40 * UI_SCALE)
ToggleLabel.Text = "Anti TP Bat"

local SwitchBg = Instance.new("Frame")
SwitchBg.Name = "SwitchBg"
SwitchBg.ZIndex = 6
SwitchBg.AnchorPoint = Vector2.new(1, 0.5)
SwitchBg.Position = UDim2.new(1, -12 * UI_SCALE, 0.5, 0)
SwitchBg.Size = UDim2.new(0, 48 * UI_SCALE, 0, 24 * UI_SCALE)
SwitchBg.BackgroundColor3 = COLORS.Off
SwitchBg.BorderSizePixel = 0
SwitchBg.Parent = ToggleRow
Instance.new("UICorner", SwitchBg).CornerRadius = UDim.new(0, 12 * UI_SCALE)

local SwitchKnob = Instance.new("Frame")
SwitchKnob.Name = "SwitchKnob"
SwitchKnob.ZIndex = 7
SwitchKnob.Position = UDim2.new(0, 3 * UI_SCALE, 0.5, -9 * UI_SCALE)
SwitchKnob.Size = UDim2.new(0, 18 * UI_SCALE, 0, 18 * UI_SCALE)
SwitchKnob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
SwitchKnob.BorderSizePixel = 0
SwitchKnob.Parent = SwitchBg
Instance.new("UICorner", SwitchKnob).CornerRadius = UDim.new(1, 0)

local SwitchHit = Instance.new("TextButton")
SwitchHit.Name = "SwitchHit"
SwitchHit.ZIndex = 8
SwitchHit.Size = UDim2.new(1, 0, 1, 0)
SwitchHit.BackgroundTransparency = 1
SwitchHit.Text = ""
SwitchHit.AutoButtonColor = false
SwitchHit.Parent = ToggleRow

local BindRow, BindLabel = makeRow("BindRow", 100 * UI_SCALE, 40 * UI_SCALE)
BindLabel.Text = "Toggle Keybind"

local BindButton = Instance.new("TextButton")
BindButton.Name = "BindButton"
BindButton.ZIndex = 6
BindButton.AnchorPoint = Vector2.new(1, 0.5)
BindButton.Position = UDim2.new(1, -12 * UI_SCALE, 0.5, 0)
BindButton.Size = UDim2.new(0, 96 * UI_SCALE, 0, 24 * UI_SCALE)
BindButton.BackgroundColor3 = COLORS.Card
BindButton.BackgroundTransparency = 0.1
BindButton.BorderSizePixel = 0
BindButton.Text = "F"
BindButton.TextColor3 = COLORS.Text
BindButton.TextSize = 11 * FONT_SCALE
BindButton.Font = Enum.Font.GothamBold
BindButton.AutoButtonColor = false
BindButton.Parent = BindRow

local BindCorner = Instance.new("UICorner")
BindCorner.CornerRadius = UDim.new(0, 8 * UI_SCALE)
BindCorner.Parent = BindButton

local BindStroke = Instance.new("UIStroke")
BindStroke.Color = COLORS.Accent
BindStroke.Thickness = 1 * UI_SCALE
BindStroke.Transparency = 0.4
BindStroke.Parent = BindButton

local StatusRow = Instance.new("Frame")
StatusRow.Name = "StatusRow"
StatusRow.ZIndex = 4
StatusRow.Position = UDim2.new(0, 12 * UI_SCALE, 1, -30 * UI_SCALE)
StatusRow.Size = UDim2.new(1, -24 * UI_SCALE, 0, 14 * UI_SCALE)
StatusRow.BackgroundTransparency = 1
StatusRow.Parent = Main

local StatusDot = Instance.new("Frame")
StatusDot.Name = "StatusDot"
StatusDot.ZIndex = 6
StatusDot.AnchorPoint = Vector2.new(0, 0.5)
StatusDot.Position = UDim2.new(0, 2 * UI_SCALE, 0.5, 0)
StatusDot.Size = UDim2.new(0, 8 * UI_SCALE, 0, 8 * UI_SCALE)
StatusDot.BackgroundColor3 = COLORS.Off
StatusDot.BorderSizePixel = 0
StatusDot.Parent = StatusRow
Instance.new("UICorner", StatusDot).CornerRadius = UDim.new(1, 0)

local StatusText = Instance.new("TextLabel")
StatusText.Name = "StatusText"
StatusText.ZIndex = 6
StatusText.Position = UDim2.new(0, 16 * UI_SCALE, 0, 0)
StatusText.Size = UDim2.new(1, -16 * UI_SCALE, 1, 0)
StatusText.BackgroundTransparency = 1
StatusText.Text = "Inactive"
StatusText.TextColor3 = COLORS.Sub
StatusText.Font = Enum.Font.Gotham
StatusText.TextSize = 10 * FONT_SCALE
StatusText.TextXAlignment = Enum.TextXAlignment.Left
StatusText.Parent = StatusRow

local KEY_ALIASES = {
	ButtonA = "A", ButtonB = "B", ButtonX = "X", ButtonY = "Y",
	ButtonL1 = "LB", ButtonR1 = "RB", ButtonL2 = "LT", ButtonR2 = "RT",
	ButtonL3 = "LS", ButtonR3 = "RS",
	ButtonStart = "Start", ButtonSelect = "Select",
	DPadUp = "D-Up", DPadDown = "D-Down", DPadLeft = "D-Left", DPadRight = "D-Right",
	LeftControl = "L-Ctrl", RightControl = "R-Ctrl",
	LeftShift = "L-Shift", RightShift = "R-Shift",
	LeftAlt = "L-Alt", RightAlt = "R-Alt",
	Return = "Enter", Escape = "Esc", Backspace = "Back",
	Insert = "Ins", Delete = "Del",
	PageUp = "PgUp", PageDown = "PgDn",
	CapsLock = "Caps", Tab = "Tab", Space = "Space",
	Zero = "0", One = "1", Two = "2", Three = "3", Four = "4",
	Five = "5", Six = "6", Seven = "7", Eight = "8", Nine = "9",
}

local function formatKey(code)
	local name = code.Name
	return KEY_ALIASES[name] or name
end

local function refresh()
	local on = AntiTpEnabled
	local color = on and COLORS.On or COLORS.Off
	tween(SwitchBg, 0.18, { BackgroundColor3 = color })
	tween(SwitchKnob, 0.18, { Position = on and UDim2.new(1, -21 * UI_SCALE, 0.5, -9 * UI_SCALE) or UDim2.new(0, 3 * UI_SCALE, 0.5, -9 * UI_SCALE) })
	StatusDot.BackgroundColor3 = color
	StatusText.Text = on and "Active" or "Inactive"
	StatusText.TextColor3 = on and COLORS.On or COLORS.Sub
end

local function toggleAntiTp()
	setAntiTp(not AntiTpEnabled)
	refresh()
end

SwitchHit.MouseButton1Click:Connect(toggleAntiTp)

local Capturing = false

BindButton.MouseButton1Click:Connect(function()
	if Capturing then return end
	Capturing = true
	BindButton.Text = "Press key..."
	BindStroke.Color = COLORS.On
	BindButton.BackgroundColor3 = COLORS.On
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if Capturing then
		if input.KeyCode ~= Enum.KeyCode.Unknown then
			Bind = input.KeyCode
			Capturing = false
			BindButton.Text = formatKey(Bind)
			BindButton.BackgroundColor3 = COLORS.Card
			BindStroke.Color = COLORS.Accent
			saveConfig()
		end
		return
	end
	if gameProcessed then return end
	if input.KeyCode == Bind then
		toggleAntiTp()
	end
end)

local minimized = false
MinBtn.MouseButton1Click:Connect(function()
	minimized = not minimized
	if minimized then
		ToggleRow.Visible = false
		BindRow.Visible = false
		StatusRow.Visible = false
		tween(Main, 0.25, { Size = UDim2.new(0, BASE_WIDTH, 0, MINIMIZED_HEIGHT) })
		MinBtn.Text = "+"
	else
		ToggleRow.Visible = true
		BindRow.Visible = true
		StatusRow.Visible = true
		tween(Main, 0.25, { Size = UDim2.new(0, BASE_WIDTH, 0, BASE_HEIGHT) })
		MinBtn.Text = "-"
	end
end)

CloseBtn.MouseButton1Click:Connect(function()
	tween(Main, 0.2, { Size = UDim2.new(0, BASE_WIDTH, 0, 0) })
	task.delay(0.22, function()
		runtime.destroy()
	end)
end)

do
	local dragging, dragStart, startPos, releaseConn
	TopBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = Main.Position
			releaseConn = input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
					if releaseConn then
						releaseConn:Disconnect()
						releaseConn = nil
					end
				end
			end)
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if not dragging then return end
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			local delta = input.Position - dragStart
			Main.Position = UDim2.new(
				startPos.X.Scale, startPos.X.Offset + delta.X,
				startPos.Y.Scale, startPos.Y.Offset + delta.Y
			)
		end
	end)
end

loadConfig()
BindButton.Text = formatKey(Bind)
refresh()

if AntiTpEnabled then
	task.defer(function()
		setAntiTp(true)
		refresh()
	end)
end

runtime.destroy = function()
	runtime.alive = false
	runtime.enabled = false
	stopStepConnection()
	restoreReplicationRoot()
	destroyFakeRoot()
	pcall(function() ScreenGui:Destroy() end)
	if environment.__NSFW_ANTITP_RUNTIME == runtime then
		environment.__NSFW_ANTITP_RUNTIME = nil
	end
end