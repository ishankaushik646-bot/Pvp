
local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local gui=Instance.new("ScreenGui")
gui.Name="OrvynHubGUI"
gui.ResetOnSpawn=false
gui.Parent=LP:WaitForChild("PlayerGui")

local main=Instance.new("Frame", gui)
main.Size=UDim2.new(0,400,0,500)
main.Position=UDim2.new(0.5,-200,0.5,-250)
main.BackgroundColor3=Color3.fromRGB(8,14,10)
Instance.new("UICorner", main).CornerRadius=UDim.new(0,20)
local s=Instance.new("UIStroke", main)
s.Color=Color3.fromRGB(0,210,210)
s.Thickness=2

local title=Instance.new("TextLabel", main)
title.Size=UDim2.new(1,0,0,50)
title.Text="ORVYN HUB - DUELS V2 STYLE TEST"
title.TextColor3=Color3.fromRGB(0,210,210)
title.BackgroundTransparency=1
title.Font=Enum.Font.GothamBlack
title.TextSize=14

local btn=Instance.new("TextButton", main)
btn.Size=UDim2.new(0,100,0,40)
btn.Position=UDim2.new(0.5,-50,0.5,-20)
btn.Text="TEST OK - Duels GUI Works!"
btn.BackgroundColor3=Color3.fromRGB(0,210,210)
btn.MouseButton1Click:Connect(function() btn.Text="Delta Works!" end)

warn("[TEST] Duels GUI shell loaded in Delta")
